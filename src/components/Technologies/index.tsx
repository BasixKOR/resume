import { Box, Heading, BoxProps } from '@chakra-ui/core';

function Technologies(props: BoxProps): JSX.Element {
  return (
    <Box {...props}>
      <Heading>Technologies</Heading>
    </Box>
  );
}

export default Technologies;
